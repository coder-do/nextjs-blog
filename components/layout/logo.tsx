import * as React from 'react';
import styles from '@/layoutStyle/logo.module.css';

const Logo: React.FC<React.ReactNode> = (): JSX.Element => <div className={styles.logo}>Meruzh' Next Blog</div>

export default Logo;